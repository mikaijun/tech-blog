scalar DateTime

type Query {
  users: [User]
  user(id: Int!): User
}

type Mutation {
  saveUser(input: UserSaveInput!): User!
}

type User {
  id: Int!
  name: String!
  createdAt: DateTime!
  deletedAt: DateTime
}

input UserSaveInput {
  id: Int
  name: String!
}
